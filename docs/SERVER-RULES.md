# قواعد سمت سرور (مشخصات مرجع)

کد `src/store` پیاده‌سازی مرجع این قواعد است و آزمون‌های `scripts/*.ts` رفتار آن را قفل می‌کنند.

- **در حالت نمایشی:** همین کد در مرورگر اجرا می‌شود.
- **در حالت وردپرس:** مرورگر فقط با آن ورودی را اعتبارسنجی می‌کند. اجرای واقعی کار سرور (`paydar-portal`) است و باید همین قواعد را پیاده کند.

فایل‌های مرجع:

| فایل | محتوا |
|---|---|
| `src/store/postingRules.ts` | قاعده ثبت هر رویداد |
| `src/store/postingEngine.ts` | کنترل توازن، سال بسته، موجودی نقد، شماره‌گذاری |
| `src/store/workflows.ts` | مراحل و نقش هر مرحله |
| `src/utils/permissions.ts` | ماتریس نقش و تفکیک وظایف |

## ۱. نقش‌ها و دسترسی

| نقش | دامنه |
|---|---|
| مدیر سیستم | همه عملیات |
| مدیر ارشد | همه عملیات |
| حسابدار | همه پروژه‌ها و ستاد |
| مدیر پروژه | فقط پروژه‌های خودش |

### محدودیت‌های مدیر پروژه

- رکورد **بدون پروژه** (ستادی) برای مدیر پروژه «خارج از دامنه» است. شامل این موارد است:
  - مانده بانک‌ها و صندوق‌ها؛
  - کدینگ با مانده؛
  - تنخواه ستاد و انبار ستاد؛
  - اسناد بدون پروژه.
- دسترسی به این رکوردها نه در لیست‌ها داده می‌شود و نه اقدامی روی آن‌ها مجاز است.

### ماتریس نقش

ماتریس کامل در `ROLE_PERMISSIONS` آمده است. اقدامات کلیدی:

| اقدام | نقش مجاز |
|---|---|
| تهیه و تأیید مشاور صورت‌وضعیت کارفرما | مدیر پروژه |
| تأیید کارفرما (ثبت درآمد) | حسابدار |
| اندازه‌گیری، تأیید کارگاه و تأیید مدیر پروژه صورت‌وضعیت جزء | مدیر پروژه |
| تأیید مالی صورت‌وضعیت جزء | حسابدار |
| تأیید نهایی صورت‌وضعیت جزء | مدیر ارشد |
| تأیید درخواست پرداخت | مدیر ارشد |
| اجرای پرداخت | حسابدار |
| تأیید فاکتور خرید، تأیید حقوق، سند دستی و معکوس | حسابدار (و مدیران) |
| بستن سال | مدیر ارشد / مدیر سیستم |
| تنظیمات | مدیر ارشد / مدیر سیستم |

### تفکیک وظایف (غیرقابل لغو)

1. هیچ کاربری رکوردی را که خودش ساخته تأیید نمی‌کند. مقایسه با **شناسه کاربر** (`user_id`) است، نه نام نمایشی.
2. یک کاربر دو مرحله **پشت‌سرهم** یک سند را تأیید نمی‌کند. این قاعده برای مدیر ارشد و مدیر سیستم هم صادق است.
3. پرداخت‌کننده درخواست پرداخت ≠ تأییدکننده آن.
4. تأییدکننده حواله انبار ≠ درخواست‌کننده. تأییدکننده فاکتور خرید و حقوق هم ثبت و با سازنده مقایسه می‌شود.
5. `window.PaydarPortal.can` فقط می‌تواند دسترسی را **محدودتر** کند و ماتریس نقش را لغو نمی‌کند.

### نکته نیازمند تأیید حسابدار

- **مصالح تحویلی کارفرما:** اکنون به‌جای دارایی (11306) به حساب کسر درآمد 41102 می‌رود. پیش از استفاده رسمی، حسابدار باید این طبقه‌بندی را با رویه شرکت تأیید کند.
- **کدهای جدید:** این کدها در کدینگ نمونه اضافه شده‌اند و باید در کدینگ افزونه هم باشند:
  - 21203 و 21204 (مالیات حقوق، مالیات تکلیفی پیمانکاران)؛
  - 41102 (مصالح تحویلی کارفرما، کسر درآمد)؛
  - 51102 (مغایرت قیمت خرید)؛
  - 62402 (کسری صندوق و تنخواه).

## ۲. مراحل تأیید

| سند | مراحل (نقش) | اثر مالی |
|---|---|---|
| صورت‌وضعیت کارفرما | تهیه (مدیر پروژه) ← ارسال به مشاور (مدیر پروژه) ← تأیید مشاور (مدیر پروژه) ← تأیید کارفرما (حسابدار) | در تأیید کارفرما: `CLIENT_STATEMENT_APPROVED` و افزایش مبلغ صورت‌وضعیت‌شده قرارداد. با رد، کاهش. |
| صورت‌وضعیت جزء | اندازه‌گیری ← تأیید کارگاه ← تأیید مدیر پروژه (هر سه مدیر پروژه) ← تأیید مالی (حسابدار) ← تأیید مدیر ارشد | در تأیید نهایی: `SUBCONTRACTOR_STATEMENT_APPROVED` و درخواست پرداخت خالص |
| هزینه تنخواه | زنجیره از تنظیمات بر اساس مبلغ: سطح ۱ [حسابدار]، سطح ۲ [مدیر پروژه، حسابدار]، سطح ۳ [مدیر پروژه، حسابدار، مدیر ارشد] | آخرین مرحله: `PETTY_CASH_EXPENSE_APPROVED` |
| درخواست خرید | تأیید کارگاه (مدیر پروژه) ← مدیر پروژه ← تدارکات (حسابدار) ← مدیر ارشد | — |
| فاکتور خرید | تطبیق سه‌جانبه و تأیید (حسابدار). فاکتور «دارای مغایرت و متوقف» قابل تأیید نیست. | `VENDOR_INVOICE` و درخواست پرداخت |
| درخواست پرداخت | تأیید (مدیر ارشد) ← پرداخت (حسابدار، کاربر دیگر) | `TREASURY_PAYMENT` |
| حقوق | تأیید مالی دوره (حسابدار) | `PAYROLL_APPROVED` و درخواست پرداخت خالص |
| حواله انبار | درخواست ← تأیید (کاربر دیگر) | `STORE_ISSUE` به بهای میانگین موزون |
| سند دستی | پیش‌نویس ← قطعی (کاربر دیگر) | — |

## ۳. قواعد ثبت رویدادها

قواعد عمومی:
- همه مبالغ ریال صحیح‌اند و سند باید متوازن باشد (جمع بدهکار = جمع بستانکار > ۰).
- هر رویداد با کلید (ماژول منبع، شناسه منبع، نوع) فقط یک بار ثبت می‌شود.
- سند در سال بسته پذیرفته نمی‌شود.

| رویداد | بدهکار | بستانکار |
|---|---|---|
| `GOODS_RECEIPT` | موجودی 11501 | کالای دریافتی فاکتورنشده 21401 (ارزش رسید) |
| `VENDOR_INVOICE` | 21401 به **ارزش رسیدها**؛ اختلاف قیمت فاکتور با رسید ← مغایرت قیمت خرید 51102؛ ارزش افزوده خرید 11304 | بستانکاران تجاری 21101 (جمع فاکتور) |
| `STORE_ISSUE` | مصالح مصرفی پروژه 51101 | موجودی 11501 (میانگین موزون) |
| `CLIENT_STATEMENT_APPROVED` | مطالبات 11201 (خالص)؛ کسورات: سپرده حسن انجام کار 11301، بیمه 11302، مالیات تکلیفی 11305، ارزش افزوده مکسوره 11307، علی‌الحساب 21302، پیش‌دریافت 21301، جریمه 62301؛ **مصالح تحویلی کارفرما: کسر درآمد 41102** | درآمد پیمان 41101 (بدون ارزش افزوده)؛ ارزش افزوده فروش 21202 |
| `SUBCONTRACTOR_STATEMENT_APPROVED` | هزینه پیمانکاران 51301 (ناخالص = Σ مقدار × نرخ) | پرداختنی پیمانکار 21102 (خالص)؛ حسن انجام کار 21601؛ بیمه 21602؛ **مالیات تکلیفی پیمانکاران 21204**؛ سایر 21603؛ **استهلاک پیش‌پرداخت 11402** |
| `PAYROLL_APPROVED` | هزینه حقوق کارگاه 51201 یا ستاد 61101 به تفکیک مرکز هزینه | حقوق پرداختنی 21501؛ بیمه 21201 (سهم کارگر + کارفرما)؛ **مالیات حقوق 21203**؛ مساعده 11303 |
| `PETTY_CASH_EXPENSE_APPROVED` | حساب هزینه گروه ۵ یا ۶ | تنخواه 11103 (زیرحساب صندوق) |
| `PETTY_CASH_REPLENISHMENT` | تنخواه 11103 | بانک 11101 |
| `TREASURY_PAYMENT` | بر اساس نوع بدهی: تأمین‌کننده 21101، پیمانکار 21102، حقوق 21501، بیمه 21201، مالیات‌ها 21202/21203/21204، تنخواه 11103، **پیش‌پرداخت پیمانکار 11402**، پیش‌پرداخت خرید 11401. نوع ناشناخته ← **خطا**. | بانک 11101 یا صندوق 11102 (کنترل موجودی) |
| `TREASURY_RECEIPT` | بانک 11101 | مطالبات 11201 / پیش‌دریافت 21301 / سایر درآمد 41302 |
| `STORE_RETURN` | موجودی 11501 | مصالح مصرفی 51101 |
| `PURCHASE_RETURN` | قبل از فاکتور: 21401؛ بعد از فاکتور: بستانکاران 21101 (با ارزش افزوده) | موجودی 11501 به **بهای میانگین** (میانگین کالای باقی‌مانده تغییر نمی‌کند)؛ اختلاف قیمت خرید با میانگین ← 51102؛ بعد از فاکتور: ارزش افزوده خرید 11304 هم برگشت. مانده فاکتور و درخواست پرداخت کم می‌شود. |
| `INVENTORY_TRANSFER` | موجودی 11501 (زیرحساب انبار مقصد) | موجودی 11501 (زیرحساب انبار مبدأ)، به بهای میانگین موزون. موجودی هر انبار زیرحساب جداست. |
| `STOCKTAKE_ADJUSTMENT` | کسری: 62401 / موجودی؛ اضافه: موجودی | اضافه: 41301 — کسری و اضافه کالاهای مختلف هرگز خالص نمی‌شوند (ردیف‌های جدا) |
| `BANK_RECONCILIATION_MATCH` | واریز: بانک / برداشت: کارمزد 62101 | واریز: واریز نامشخص 21701 / برداشت: بانک. **به‌صورت پیش‌نویس، قطعی با تأیید کاربر دیگر.** |
| `JOURNAL_REVERSAL` | ردیف‌های سند اصلی با جابه‌جایی بدهکار و بستانکار | |
| `FISCAL_YEAR_CLOSE` | بستن مانده حساب‌های گروه ۴، ۵ و ۶ به سود انباشته 33 | |

### قواعد تکمیلی

- **معکوس:**
  - سند معکوس، معکوس‌کردنی نیست. سند اختتامیه هم معکوس‌کردنی نیست.
  - سند در انتظار تأیید معکوس نمی‌شود و هر سند فقط یک بار معکوس می‌شود.
  - معکوس سند خودکار فقط وقتی مجاز است که رویداد منبع هم برگشت بخورد. رویداد اصلی دیگر «ثبت‌شده» نمی‌ماند تا ثبت درست بعدی ممکن باشد.
- **بستن سال:**
  - فقط سال‌های گذشته و به ترتیب بسته می‌شوند.
  - تاریخ سند اختتامیه آخرین روز واقعی سال است (۱۲/۳۰ در سال کبیسه).
  - سند اختتامیه در سود و زیان همان سال لحاظ نمی‌شود.
- **تاریخ سند:**
  - تاریخ سند رویداد خودکار، تاریخ تأیید یا ثبت است، نه تاریخ تهیه سند منبع.
  - کاربر مجاز می‌تواند تاریخ دیگری داخل سال باز انتخاب کند.
- **شماره سند:**
  - برای هر سال مالی از ۱ شروع می‌شود و به ترتیب تاریخ است، با طول ثابت: `ACC-YYYY-NNNNN`.
  - شماره را فقط سرور می‌دهد؛ شماره ارسالی کلاینت (حواله، سند) نادیده گرفته می‌شود.
- **سند دستی:** فقط حساب تفصیلی یا معین برگ (بدون زیرحساب) پذیرفته می‌شود و حساب گروه یا کل رد می‌شود.
- **مغایرت تنخواه:** مانده مورد انتظار = مانده دفتری − هزینه‌های در انتظار تأیید. اختلاف شمارش به سند پیش‌نویس تبدیل می‌شود:
  - کسری: 62402 / تنخواه؛
  - اضافه: تنخواه / 41302.
- **قرارداد:** مبلغ صورت‌وضعیت‌شده یا اجراشده قرارداد فقط با تأیید صورت‌وضعیت بالا می‌رود و با رد برمی‌گردد. در پیش‌نویس تغییر نمی‌کند.
- **انبار:**
  - برگشت به تأمین‌کننده، میانگین موزون را از نو محاسبه می‌کند و آن را صفر نمی‌کند.
  - خروج بیش از موجودی آزاد ممکن نیست و ردیف‌های یک کالا جمع زده می‌شوند.
